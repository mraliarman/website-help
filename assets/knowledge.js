const root=document.querySelector('#knowledgeRoot');
const escapeHTML=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[char]));
const normalize=value=>String(value||'').normalize('NFC').trim().toLocaleLowerCase('fa-IR');
const params=new URLSearchParams(location.search);
const requested=normalize(params.get('term'));
const allConcepts=Object.entries(concepts).flatMap(([category,items])=>items.map(term=>({term,category})));
const selected=allConcepts.find(item=>normalize(item.term)===requested);
const concept=selected||allConcepts[0];
const meta=categoryMeta[concept.category]||{label:concept.category,icon:'•'};
const source=sources[concept.category]||sources.wordpress;
const related=allConcepts.filter(item=>item.category===concept.category&&item.term!==concept.term).slice(0,12);
const knowledgeUrl=term=>`/knowledge.html?term=${encodeURIComponent(term)}`;
const safeId=value=>encodeURIComponent(String(value)).replace(/%/g,'-');
const baseDefinition=`${concept.term} یک مفهوم تخصصی در حوزه «${meta.label}» است. در یک پروژه حرفه‌ای، این مفهوم باید نه فقط از نظر تعریف، بلکه از منظر معماری، پیاده‌سازی، امنیت، عملکرد، دسترس‌پذیری، SEO، تست و نگهداری بررسی شود.`;
const workflow=[['تعریف و جایگاه',`ابتدا مشخص کنید «${concept.term}» دقیقا چه مسئله‌ای را حل می‌کند، ورودی و خروجی آن چیست و در کدام لایه از معماری وب قرار می‌گیرد.`],['طراحی',`قبل از پیاده‌سازی، قراردادها، وابستگی‌ها، مرز مسئولیت و رفتار مورد انتظار «${concept.term}» را مشخص کنید. هدف این است که تصمیم امروز به بدهی فنی فردا تبدیل نشود.`],['پیاده‌سازی',`پیاده‌سازی «${concept.term}» باید با مستندات رسمی اکوسیستم، قراردادهای API و استانداردهای پروژه هماهنگ باشد. تغییرات را کوچک، قابل تست و قابل بازگشت نگه دارید.`],['اعتبارسنجی',`ورودی‌ها و پیش‌شرط‌های مرتبط با «${concept.term}» را در مرز سیستم اعتبارسنجی کنید و هیچ داده غیرقابل اعتماد را بدون کنترل وارد لایه حساس نکنید.`],['تست',`برای «${concept.term}» حداقل یک مسیر موفق، مسیر خطا، ورودی مرزی و سناریوی regression تعریف کنید و در صورت امکان تست را وارد CI کنید.`],['Production',`در محیط واقعی، تنظیمات، لاگ، مانیتورینگ، cache، backup و rollback را پیش از انتشار مشخص کنید تا خطا قابل مشاهده و قابل بازیابی باشد.`]];
const coreSections=[['تعریف دقیق',baseDefinition],['چرا مهم است؟',`درک درست «${concept.term}» مستقیما روی کیفیت تصمیم‌های مهندسی اثر می‌گذارد. استفاده نادرست می‌تواند باعث پیچیدگی، آسیب‌پذیری، افت Performance، خطای SEO یا تجربه کاربری ضعیف شود.`],['نحوه کار',`مدل ذهنی مناسب این است که «${concept.term}» را بخشی از یک زنجیره در نظر بگیرید: داده یا درخواست وارد سیستم می‌شود، قوانین و قراردادها اعمال می‌شوند، نتیجه تولید یا ذخیره می‌شود و سپس خروجی توسط مصرف‌کننده بعدی استفاده می‌شود. در هر مرحله باید مرز اعتماد، خطا و مسئولیت روشن باشد.`],['کاربرد در پروژه واقعی',`در پروژه‌های واقعی، «${concept.term}» معمولا در کنار چند مفهوم دیگر استفاده می‌شود. هنگام طراحی، وابستگی‌های مستقیم و غیرمستقیم را مشخص کنید و از ایجاد coupling غیرضروری جلوگیری کنید.`],['بهترین روش',`برای استفاده حرفه‌ای از «${concept.term}»، به مستندات رسمی اولویت بدهید، قراردادهای پروژه را ثابت نگه دارید، رفتارهای edge case را تست کنید، خطاها را ثبت کنید و تصمیم‌های معماری مهم را مستند کنید.`],['امنیت',`هر جا «${concept.term}» با ورودی کاربر، احراز هویت، مجوز، فایل، شبکه، پایگاه‌داده یا خروجی HTML درگیر است، اصل least privilege، validation، sanitization و escaping را متناسب با context اعمال کنید.`],['Performance',`اثر «${concept.term}» را با داده واقعی اندازه‌گیری کنید. کاهش درخواست‌های غیرضروری، جلوگیری از کار تکراری، cache مناسب، payload کوچک و حذف پردازش‌های blocking از اصول عمومی بهینه‌سازی هستند.`],['SEO',`اگر «${concept.term}» در خروجی قابل مشاهده سایت یا ساختار URL، لینک، metadata، rendering و indexability اثر دارد، نتیجه نهایی را از دید crawler و کاربر بررسی کنید و canonical، status code و محتوای قابل مشاهده را هم‌راستا نگه دارید.`],['Accessibility',`خروجی مرتبط با «${concept.term}» باید برای keyboard، screen reader، zoom، contrast و کاربران دارای محدودیت حرکتی یا دیداری قابل استفاده باشد. Semantic HTML و نام‌گذاری صحیح تعاملات را در اولویت قرار دهید.`],['عیب‌یابی',`برای خطای مرتبط با «${concept.term}»، ابتدا مسئله را reproducible کنید، سپس ورودی، configuration، dependency، network، log و تغییرات اخیر را بررسی کنید. به جای workaround، علت ریشه‌ای را پیدا و اصلاح کنید.`],['مانیتورینگ',`برای محیط Production، شاخص‌هایی مانند error rate، latency، throughput، resource usage و رفتار غیرعادی را متناسب با نوع «${concept.term}» پایش کنید و برای خطاهای مهم alert تعریف کنید.`],['نسخه‌بندی و مهاجرت',`هر تغییر نسخه‌ای که رفتار «${concept.term}» را عوض می‌کند باید با release note، تست regression و برنامه rollback همراه باشد. تغییرات ناسازگار را قبل از انتشار شناسایی کنید.`]];
const sectionFromFacet=(facet,text)=>`<section class="knowledge-section" id="${safeId(facet)}"><h2>${escapeHTML(facet)}</h2><p>${escapeHTML(concept.term)}: ${escapeHTML(text)}</p><p>در اجرای عملی، این موضوع را در کنار قراردادهای پروژه، نسخه نرم‌افزار، محیط اجرا و نیازهای واقعی محصول بررسی کنید. هر تصمیم مهم باید قابل اندازه‌گیری، تست و بازبینی باشد.</p></section>`;
const renderCode=()=>{const category=concept.category;if(category==='html')return '<div class="knowledge-code">&lt;article&gt;\n    &lt;h1&gt;عنوان محتوا&lt;/h1&gt;\n    &lt;p&gt;محتوای معنایی و قابل دسترس.&lt;/p&gt;\n&lt;/article&gt;</div>';if(category==='css')return '<div class="knowledge-code">.component {\n    display: grid;\n    gap: 1rem;\n    max-width: 70rem;\n}</div>';if(category==='javascript'||category==='webapi')return '<div class="knowledge-code">const result = await fetch(url);\nif (!result.ok) {\n    throw new Error(`HTTP ${result.status}`);\n}\nconst data = await result.json();</div>';if(category==='php'||category==='wordpress'||category==='plugins'||category==='themes')return '<div class="knowledge-code">&lt;?php\nfunction example(): void {\n    wp_safe_redirect( home_url( \'/\' ) );\n    exit;\n}</div>';if(category==='database')return '<div class="knowledge-code">SELECT id, name\nFROM users\nWHERE status = \'active\'\nORDER BY id DESC;</div>';return ''};
const render=()=>{const facetList=[...(typeof facets!=='undefined'?facets:[])];const advancedList=[...(typeof advancedFacets!=='undefined'?advancedFacets:[])];const facetSections=facetList.map(facet=>sectionFromFacet(facet,(typeof facetText!=='undefined'&&facetText[facet])||'این جنبه باید متناسب با معماری، نسخه و سناریوی واقعی پروژه بررسی شود.')).join('');const advancedSections=advancedList.map(facet=>sectionFromFacet(facet,(typeof advancedFacetText!=='undefined'&&advancedFacetText[facet])||'این موضوع بخشی از بررسی حرفه‌ای و تصمیم‌گیری مهندسی است.')).join('');const workflowHTML=workflow.map(([title,text])=>`<div class="knowledge-item"><strong>${escapeHTML(title)}</strong><span>${escapeHTML(text)}</span></div>`).join('');const coreHTML=coreSections.map(([title,text])=>`<section class="knowledge-section"><h2>${escapeHTML(title)}</h2><p>${escapeHTML(text)}</p></section>`).join('');const relatedHTML=related.map(item=>`<a href="${knowledgeUrl(item.term)}">${escapeHTML(item.term)}</a>`).join('');const code=renderCode();root.innerHTML=`<section class="knowledge-hero"><a class="knowledge-back" href="/">← بازگشت به دانشنامه</a><span class="knowledge-eyebrow">${escapeHTML(meta.label)} / KNOWLEDGE SINGLE</span><h1 class="knowledge-title">${escapeHTML(concept.term)} <em>— راهنمای تخصصی</em></h1><p class="knowledge-summary">${escapeHTML(baseDefinition)} این صفحه به صورت یک مدخل مستقل طراحی شده تا تعریف، معماری، پیاده‌سازی، امنیت، Performance، SEO، Accessibility، تست، عیب‌یابی و استقرار را در یک مرجع یکپارچه پوشش دهد.</p><div class="knowledge-meta"><span class="knowledge-pill">${escapeHTML(meta.icon)} ${escapeHTML(meta.label)}</span><span class="knowledge-pill">مرجع رسمی</span><span class="knowledge-pill">راهنمای Production</span><span class="knowledge-pill">فارسی / RTL</span></div></section><div class="knowledge-layout"><article class="knowledge-main">${coreHTML}<section class="knowledge-section"><h2>الگوی اجرای حرفه‌ای</h2><div class="knowledge-grid">${workflowHTML}</div></section>${code?`<section class="knowledge-section"><h2>نمونه ساختار فنی</h2>${code}<p>این نمونه صرفا الگوی آموزشی است و باید با قرارداد، نسخه و معماری پروژه واقعی تطبیق داده شود.</p></section>`:''}<section class="knowledge-section"><h2>مفاهیم مرتبط</h2><div class="knowledge-related">${relatedHTML}</div></section><section class="knowledge-section"><h2>لایه‌های تخصصی</h2>${facetSections}</section><section class="knowledge-section"><h2>بررسی پیشرفته</h2>${advancedSections}</section><section class="knowledge-section"><h2>مرجع اصلی و مطالعه بیشتر</h2><p>برای جزئیات API، قراردادها، محدودیت‌های نسخه و تغییرات رسمی، همیشه مستندات منبع اصلی را بر منابع ثانویه مقدم بدانید.</p><a class="button primary knowledge-source" href="${escapeHTML(source)}" target="_blank" rel="noopener noreferrer">مطالعه منبع رسمی ↗</a></section></article><aside class="knowledge-aside"><div class="knowledge-nav"><strong>فهرست صفحه</strong><a href="#تعریف-دقیق">تعریف و جایگاه</a><a href="#امنیت">امنیت</a><a href="#Performance">Performance</a><a href="#SEO">SEO</a><a href="#Accessibility">Accessibility</a><a href="#عیب‌یابی">عیب‌یابی</a><a href="#مفاهیم-مرتبط">مفاهیم مرتبط</a><a href="#مرجع-اصلی-و-مطالعه-بیشتر">مرجع اصلی</a></div></aside></div>`;document.title=`${concept.term} | Website Help`;const description=`راهنمای فارسی تخصصی ${concept.term} در حوزه ${meta.label} شامل تعریف، پیاده‌سازی، امنیت، Performance، SEO، Accessibility، تست و عیب‌یابی.`;document.querySelector('meta[name="description"]').setAttribute('content',description);document.querySelector('link[rel="canonical"]').href=location.href;const schema=document.createElement('script');schema.type='application/ld+json';schema.textContent=JSON.stringify({'@context':'https://schema.org','@type':'TechArticle','headline':`${concept.term} — راهنمای تخصصی`,'description':description,'inLanguage':'fa-IR','url':location.href,'isPartOf':{'@type':'WebSite','name':'Website Help','url':'https://help.bestjustify.ir/'}});document.head.appendChild(schema)};
if(requested&&selected)render();else root.innerHTML='<section class="knowledge-error"><h1>مدخل پیدا نشد</h1><p>مفهوم در پایگاه دانش موجود نیست.</p><a class="button primary" href="/">بازگشت به دانشنامه</a></section>';